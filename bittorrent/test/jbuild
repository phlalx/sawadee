(jbuild_version 1)

(executable
   ((name test_krpc_packet)
      (libraries (bittorrent core ppx_jane oUnit)) 
      (preprocess (pps (ppx_jane)))))

 (alias
 ((name    runtest)
  (deps    (test_krpc_packet.bc))
  (action  (run ${<}))))

