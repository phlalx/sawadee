(jbuild_version 1)

(executables
   ((names (main test_bitset test_krpc_packet tracker_server))
      (libraries (core core_extended ppx_jane uri cohttp async cohttp.async 
                  bencode sha oUnit hex)) 
      (preprocess (pps (ppx_jane)))))


(alias
 ((name    runtest)
  (deps    (test_bitset.bc))
  (action  (run ${<}))))

 (alias
 ((name    runtest)
  (deps    (test_krpc_packet.bc))
  (action  (run ${<}))))
